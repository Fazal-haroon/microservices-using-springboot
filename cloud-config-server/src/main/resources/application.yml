server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Fazal-haroon/microservices-using-springboot
          clone-on-start: true
